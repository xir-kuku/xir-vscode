{
  "//": "Bind shared assembly suffixes to XIRASM in this workspace when another extension claims them globally.",
  "files.associations": {
    "*.xir": "xirasm",
    "*.asm": "xirasm",
    "*.inc": "xirasm"
  }
}
